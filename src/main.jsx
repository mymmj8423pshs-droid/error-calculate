import React from "react";
import ReactDOM from "react-dom/client";
import ErrorCalculator from "./ErrorCalculator.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ErrorCalculator />
  </React.StrictMode>
);
